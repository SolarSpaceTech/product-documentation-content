# Подробное описание мета свойства `categoryOrder`

**Мета свойство:** `categoryOrder`

**Тип данных:** Число

---

## Описание

Мета свойство `categoryOrder` используется для определения порядка отображения статьи внутри определённой секции на главной странице сайта. Оно позволяет точно настроить последовательность статей в секции, обеспечивая контроль над тем, какая статья будет показана первой, второй и далее.

---

## Основные функции

- **Управление порядком статей в секции:** Позволяет упорядочить статьи в желаемой последовательности внутри одной секции на главной странице.
- **Приоритет отображения:** Статьи с меньшим значением `categoryOrder` отображаются выше в списке секции, предоставляя им более высокий приоритет.

---

## Как работает вместе с другими мета свойствами

- **`category`:** Определяет секцию на главной странице, в которой будет отображаться статья. `categoryOrder` применяется только к статьям внутри одной и той же секции.
- **`categoryName`:** Задаёт название статьи, отображаемое в секции на главной странице.
- **`categoryDescription`:** Предоставляет краткое описание статьи для отображения в секции.
- **`categoryIcon`:** Указывает путь к иконке, отображаемой рядом с названием статьи в секции.

---

## Важные замечания

- **Обязательность:** `categoryOrder` не является обязательным. Если свойство не указано, система может использовать другие критерии для определения порядка (например, по алфавиту).
- **Уникальность значений:** Рекомендуется использовать уникальные значения `categoryOrder` внутри одной секции для точного контроля порядка отображения.
- **Диапазон значений:** Может принимать любые целочисленные значения. Обычно используются положительные числа для простоты управления.

---

## Пример использования

### Пример 1: Статьи в секции `start`

```md
---
title: Быстрый старт
displayName: Начало работы
order: 1
published: true
category: start
categoryName: Быстрый старт
categoryDescription: Узнайте, как быстро начать пользоваться нашим сервисом
categoryOrder: 10
categoryIcon: /icons/quickstart.svg
---
# Быстрый старт

Добро пожаловать в руководство по быстрому началу работы...
```

```md
---
title: Глубокое погружение
displayName: Продвинутое использование
order: 2
published: true
category: start
categoryName: Глубокое погружение
categoryDescription: Подробное руководство для опытных пользователей
categoryOrder: 20
categoryIcon: /icons/deepdive.svg
---
# Глубокое погружение

В этом руководстве мы подробно рассмотрим...
```

В данном примере:

- В секции `start` на главной странице статья "Быстрый старт" с `categoryOrder: 10` будет отображаться перед статьёй "Глубокое погружение" с `categoryOrder: 20`.

---

## Когда следует использовать `categoryOrder`

- **Контроль последовательности:** Когда важно, чтобы статьи отображались в определённом порядке, например, от вводных материалов к более сложным.
- **Установка приоритетов:** Чтобы выделить более важные или актуальные статьи, разместив их выше в секции.
- **Организация контента:** Для логической группировки и последовательности представления информации пользователям.

---

## Советы по использованию

- **Инкрементальные шаги:** Используйте шаги между значениями (например, 10, 20, 30), чтобы было легче вставлять новые статьи между существующими без необходимости переназначения всех значений.
- **Проверка уникальности:** Убедитесь, что значения `categoryOrder` уникальны внутри секции, чтобы избежать неопределённости в порядке отображения.
- **Обдуманный порядок:** Планируйте порядок статей в соответствии с их важностью и последовательностью чтения.

---

## Часто задаваемые вопросы

### Что произойдёт, если у нескольких статей одинаковый `categoryOrder`?

Если несколько статей в одной секции имеют одинаковый `categoryOrder`, система может определить их порядок отображения на основе других критериев (например, по алфавиту `categoryName`). Чтобы избежать неопределённости, рекомендуется присваивать уникальные значения `categoryOrder`.

### Можно ли использовать отрицательные значения или ноль?

Да, можно использовать отрицательные значения и ноль. Статьи с меньшими значениями (включая отрицательные) будут отображаться выше. Однако для ясности и удобства управления рекомендуется использовать положительные числа.

### Что произойдёт, если `categoryOrder` не указан?

Если `categoryOrder` не указан, система может использовать другие методы для определения порядка отображения статей в секции, такие как алфавитный порядок по `categoryName` или порядок добавления статей.

---

## Связанные мета свойства

- **`category`**: Определяет секцию на главной странице для отображения статьи.
- **`categoryName`**: Название статьи в секции на главной странице.
- **`categoryDescription`**: Краткое описание статьи в секции.
- **`categoryIcon`**: Иконка, отображаемая рядом с названием статьи.

---

## Пример полного мета описания статьи с `categoryOrder`

```md
---
title: FAQ по настройке
displayName: Вопросы по настройке
order: 5
published: true
category: faq
categoryName: Настройка системы
categoryDescription: Ответы на частые вопросы по настройке
categoryOrder: 15
categoryIcon: /icons/settings.svg
---
# FAQ по настройке

Здесь мы ответим на самые распространённые вопросы по настройке...
```

В этом примере:

- **Секция:** Статья отображается в секции `faq`.
- **Порядок:** Значение `categoryOrder: 15` определяет позицию статьи внутри секции `faq`.
- **Отображение:** Статья будет отображаться после статей с `categoryOrder` меньше 15 и перед статьями с большим значением.

---

## Рекомендации по назначению значений `categoryOrder`

- **Планирование:** Заранее планируйте структуру секции и распределение статей, чтобы определить подходящие значения `categoryOrder`.
- **Группировка:** Если в секции есть подкатегории, можно использовать диапазоны значений для каждой группы (например, 10-19 для одной группы, 20-29 для другой).
- **Обновление:** При добавлении новых статей старайтесь сохранять логическую последовательность значений `categoryOrder`.

---

## Заключение

Мета свойство `categoryOrder` является ключевым инструментом для точного управления порядком отображения статей в секциях на главной странице. Оно позволяет создать структурированный и логичный интерфейс, улучшая навигацию и общее впечатление пользователей от сайта. Правильное использование этого свойства способствует эффективной организации контента и повышает удобство использования ресурса.

---

## Дополнительные примеры использования

### Секция `services`

```md
---
title: Наши услуги
displayName: Услуги
order: 3
published: true
category: services
categoryName: Консалтинг
categoryDescription: Профессиональные консультации для вашего бизнеса
categoryOrder: 5
categoryIcon: /icons/consulting.svg
---
# Наши услуги: Консалтинг

Мы предлагаем экспертные консультации в области...
```

```md
---
title: Техническая поддержка
displayName: Поддержка
order: 4
published: true
category: services
categoryName: Техподдержка 24/7
categoryDescription: Круглосуточная поддержка для наших клиентов
categoryOrder: 10
categoryIcon: /icons/support.svg
---
# Техническая поддержка

Наша команда готова помочь вам в любое время...
```

В этом случае:

- В секции `services` на главной странице "Консалтинг" с `categoryOrder: 5` будет отображаться перед "Техподдержка 24/7" с `categoryOrder: 10`.

---

Используйте `categoryOrder` для создания упорядоченных и привлекательных секций на главной странице, обеспечивая пользователям лёгкий доступ к наиболее важной и актуальной информации.
